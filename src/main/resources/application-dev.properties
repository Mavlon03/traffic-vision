server.port=8080

# Datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/traffic-vision-db
spring.datasource.username=postgres
spring.datasource.password=root123
spring.datasource.driverClassName=org.postgresql.Driver

# Python API
python.api.url=http://localhost:5000

# CORS / WebSocket
cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000
websocket.allowed-origin-patterns=http://localhost:3000,http://127.0.0.1:3000

# Swagger
app.swagger.description=Yo'l belgisi aniqlash tizimi API (development)
app.swagger.server-url=http://localhost:8080
app.swagger.server-description=Local development server

# Logging
spring.jpa.show-sql=true
logging.level.uz.pdp.trafficvision=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Devtools
spring.devtools.restart.enabled=true
